name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify Cargo.toml version matches tag
        run: |
          CARGO_VER="v$(grep '^version' Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')"
          if [ "$CARGO_VER" != "${{ github.ref_name }}" ]; then
            echo "::error::Cargo.toml says $CARGO_VER but tag is ${{ github.ref_name }}"
            exit 1
          fi
          echo "Version match: $CARGO_VER"

  build:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Install musl tools
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Test
        run: cargo test --target x86_64-unknown-linux-musl

      - name: Build
        run: cargo build --release --target x86_64-unknown-linux-musl

      - name: Package
        run: |
          DIR="bdemu-${{ github.ref_name }}"
          mkdir -p "$DIR/profiles"
          cp target/x86_64-unknown-linux-musl/release/bdemu "$DIR/"
          cp target/x86_64-unknown-linux-musl/release/libbdemu.so "$DIR/" 2>/dev/null || true
          cp -r profiles/* "$DIR/profiles/" 2>/dev/null || true
          tar czf "${DIR}-x86_64-linux.tar.gz" "$DIR"

      - name: Upload
        uses: softprops/action-gh-release@v2
        with:
          files: '*.tar.gz'
